---
interface HeroImage {
    src: string,
    alt: string,
    width?: number,
    height?: number
}

const {src, alt, width=1020, height=510} = Astro.props; 

---
<div class="hero-image">
    {src && <img width={width} height={height} src={src} alt={alt} />}
</div>